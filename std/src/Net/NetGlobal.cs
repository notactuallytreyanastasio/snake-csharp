using S = System;
using T = System.Threading.Tasks;
using N = TemperLang.Std.Net;
namespace TemperLang.Std.Net
{
    public static class NetGlobal
    {
        internal static T::Task<N::INetResponse> sendRequest__16(string url__35, string method__36, string ? bodyContent__37, string ? bodyMimeType__38)
        {
            throw new S::Exception();
        }
    }
}
